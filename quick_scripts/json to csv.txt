# Usage: python3 quick_scripts/json_results_to_csv_batch.py <FILE.json> [--format compact|trace] [--overwrite]
#
# Default (compact): one row per solve, list/dict fields (e.g. value_trace) dropped.
#                    CSV named <input stem>.csv — matches the older BigM/MIP layout.
# --format trace:    one row per value_trace event, trace fields prefixed `trace_`.
#                    CSV named <input stem>_trace.csv by default.
# Override the auto suffix with --suffix '' (to always use plain .csv) or any string.

# Compact (old format)
python3 quick_scripts/json_results_to_csv_batch.py \
  results_sat_scl_totalizer_infsteps180_cost_fix.json \
  --format compact --overwrite

# Trace (expand value_trace)
python3 quick_scripts/json_results_to_csv_batch.py \
  results_sat_scl_totalizer_infsteps180_cost_fix.json \
  --format trace --overwrite

# Convert multiple at once
python3 quick_scripts/json_results_to_csv_batch.py \
  results_*.json \
  --format compact --overwrite
